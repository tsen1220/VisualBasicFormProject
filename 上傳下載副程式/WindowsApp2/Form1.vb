Public Class Form1


    Sub downloadfile(
     address As String,
     destinationFileName As String,
     username As String,
     password As String,
     showUI As Boolean,
     connectiontimeout As Integer,
     overwrite As Boolean,
     onUserCancel As FileIO.UICancelOption)


    End Sub

    Sub uploadfile(
     address As String,
     destinationFileName As String,
     username As String,
     password As String,
     showUI As Boolean,
     connectiontimeout As Integer,
     overwrite As Boolean,
     onUserCancel As FileIO.UICancelOption)

    End Sub







End Class
